@model IEnumerable<student_management_asp_uppgift1.Models.StudentCourse>

@{
    ViewData["Title"] = "Index";
}

<h1>Signing Students to the Courses</h1>
<br />
<p>
    <a asp-action="Create" type="button" class="btn btn-primary">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Student)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Course)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Student.DisplayName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Course.CourseName)
            </td>
            <td>
                @*<a asp-action="Edit" asp-route-id="@item.ApplicationUserId" type="button" class="btn btn-secondary">Edit</a>*@
                <a asp-action="Details" asp-route-id="@item.ApplicationUserId" type="button" class="btn btn-info">Details </a>
                @*<a asp-action="Delete" asp-route-id="@item.ApplicationUserId" type="button" class="btn btn-warning">Delete</a>*@


                @*@Html.ActionLink("Edit", "Edit", new { id = item.ApplicationUserId }) |
                @Html.ActionLink("Details", "Details", new { id = item.ApplicationUserId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.ApplicationUserId })*@
            </td>
        </tr>
}
    </tbody>
</table>
